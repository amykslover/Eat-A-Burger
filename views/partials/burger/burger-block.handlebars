<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-eatstate="{{eaten}}">

		{{#if eaten}}DEVOUR ME!{{else}}MAKE ANOTHER{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
