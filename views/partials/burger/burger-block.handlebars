<div class='itemlist'>
	{{name}}
	<button class="delete-burger" data-id="{{this.id}}">X</button>

	<button class="change-eaten" data-id="{{id}}" data-eatstate="{{eaten}}">

		{{#if eaten}}EAT IT!{{else}}REMAKE!{{/if}}
	</button>

</div>
